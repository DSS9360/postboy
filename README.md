# Postboy

纯前端网页版 API 测试工具，类似 Postman，无需服务器，单个 HTML 文件开箱即用。

## 功能
- 7种 HTTP 方法（GET/POST/PUT/DELETE/PATCH/HEAD/OPTIONS）
- Params / Headers / Body / Auth 完整支持
- 响应格式化高亮（Pretty / Raw / Preview / Headers）
- 请求历史 + 集合管理（localStorage 持久化）
- 导入/导出集合
- 深色/浅色主题切换

## 使用
直接在浏览器中打开 `index.html` 即可。
